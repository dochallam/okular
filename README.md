# Configuration for Okcular PDF viewer
After installing the `okcular` package, clone this repository into local directory `~/.confg`

This will create two files in `~/.config`:

- `okularrc`
- `okularpartrc`
